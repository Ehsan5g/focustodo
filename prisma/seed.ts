// Seed script placeholder for feature 001 (US3).
// The schema is intentionally model-free (D8) and migration-based, so there
// is nothing to seed yet — the tasks feature will add real seeding.
// Kept as an entry point so the README's setup flow stays uniform.
console.log("Nothing to seed: the foundation schema has no models (D8).");
